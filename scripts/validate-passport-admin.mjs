import { readFile } from 'node:fs/promises';const fail=m=>{throw new Error(m)};const files={auth:'../netlify/functions/_passport-auth.mjs',request:'../netlify/functions/passport-request.mjs',verify:'../netlify/functions/passport-verify.mjs',passport:'../netlify/functions/passport.mjs',admin:'../netlify/functions/admin-overview.mjs',html:'../site/passport.html',js:'../site/assets/js/passport.js',netlify:'../netlify.toml',migration:'../netlify/database/migrations/002_passport-auth/migration.sql'};const t={};for(const[k,p]of Object.entries(files))t[k]=await readFile(new URL(p,import.meta.url),'utf8');for(const n of ['wildones-passport-magic','wildones-passport-session','FOR UPDATE','consumed_at'])if(!t.auth.includes(n))fail(`Passport auth invariant missing: ${n}`);for(const n of ['verifyTurnstile',"'passport_login'",'If a Wild Ones Passport exists'])if(!t.request.includes(n))fail(`Passport request invariant missing: ${n}`);if(!t.verify.includes('consumeMagicLink')||!t.verify.includes('passportCookie'))fail('Passport verify flow missing.');for(const n of ['readPassportSession','STORES.tickets','STORES.entitlements','checkedIn'])if(!t.passport.includes(n))fail(`Passport data boundary missing: ${n}`);if(!t.admin.includes('isAdmin')||!t.admin.includes('admissionRevenueCents')||!t.admin.includes('addonRevenueCents'))fail('Master admin boundary missing.');if(!t.js.includes('/api/passport/config')||!t.js.includes('turnstile.render'))fail('Passport runtime Turnstile config missing.');if(!t.netlify.includes('/admin/overview')||!t.netlify.includes('/passport*'))fail('Passport/admin routes or headers missing.');if(!t.migration.includes('wildones_passport_magic_links'))fail('Passport auth migration missing.');console.log('Validated Passport one-time authentication, attendee aggregation, master admin auth, runtime Turnstile, and private routing.');