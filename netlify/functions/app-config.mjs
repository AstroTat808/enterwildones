import { env } from './_env.mjs';import { resolveEvent,toPublicEvent } from './_events.mjs';import { json } from './_response.mjs';export default async(req)=>{if(req.method!=='GET')return json({error:'Method not allowed.'},405);const url=new URL(req.url);const event=resolveEvent({eventId:url.searchParams.get('eventId'),slug:url.searchParams.get('event')||url.searchParams.get('slug')});if(!event||event.visibility!=='public')return json({error:'Event not found.'},404);return json({event:toPublicEvent(event),turnstileSiteKey:env('WILDONES_TURNSTILE_SITE_KEY')||null,applicationsReady:Boolean(env('WILDONES_TURNSTILE_SITE_KEY')&&env('WILDONES_TURNSTILE_SECRET_KEY'))});};export const config={path:'/api/app-config'};