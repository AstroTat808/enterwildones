import { blobStore } from './_blob-store.mjs';
import { env } from './_env.mjs';
import { resolveEvent } from './_events.mjs';
import { readInviteAccess } from './_invitations.mjs';
import { makeCheckoutReturn } from './_checkout-return.mjs';
import { ticketPriceCents, ticketPriceEnv, ticketProductName, loadInvitation, existingTicketForInvitation } from './_payments.mjs';
import { json, readBody } from './_response.mjs';
import { sameOrigin } from './_security.mjs';
import { stripePost } from './_stripe.mjs';
import { STORES, eventKey } from './_stores.mjs';
import { writeAudit } from './_audit.mjs';

export default async(req)=>{
  if(req.method!=='POST')return json({error:'Method not allowed.'},405);if(!sameOrigin(req))return json({error:'Origin not allowed.'},403);
  let body;try{body=await readBody(req);}catch{return json({error:'Invalid checkout request.'},400);}const event=resolveEvent({eventId:body.eventId,slug:body.event});if(!event||event.visibility!=='public')return json({error:'Event not found.'},404);if(!event.ticketSalesOpen)return json({error:`Ticket sales for ${event.name} are not open.`,code:'sales_closed'},403);
  const price=ticketPriceCents(event.eventId);if(!price)return json({error:`Ticket price is not configured for ${event.name}.`,code:'price_not_configured',requiredEnv:ticketPriceEnv(event.eventId)},503);if(!env('STRIPE_SECRET_KEY')||!env('WILDONES_TICKET_ACCESS_SECRET'))return json({error:'Ticket checkout is not configured.'},503);
  const access=readInviteAccess(req,event.eventId);if(!access)return json({error:'Your invitation access is missing or expired. Redeem your event invitation again.',code:'invite_access_required'},401);
  const invitation=await loadInvitation(event.eventId,access.invitationId);if(!invitation||invitation.userId!==access.userId||invitation.applicationId!==access.applicationId||!['redeemed','fulfilled'].includes(invitation.status))return json({error:'This invitation is not eligible for checkout.'},403);if(Number(invitation.maxTickets||1)!==1)return json({error:'Multi-ticket invitations require the group-ticket identity flow and cannot use single-person checkout.',code:'group_ticket_required'},409);if(invitation.status==='fulfilled'||Number(invitation.purchaseCount||0)>=1){const existing=await existingTicketForInvitation(event.eventId,invitation.invitationId);return json({error:'This invitation has already been used for admission.',ticketId:existing?.ticketId||null},409);}
  const latestKey=eventKey(event.eventId,`checkout-latest/${invitation.invitationId}`);const payments=blobStore(STORES.payments);const latest=await payments.get(latestKey,{type:'json'}).catch(()=>null);if(latest?.status==='checkout_created'&&latest?.checkoutUrl&&new Date(latest.expiresAt||0).getTime()>Date.now()+60000)return json({ok:true,checkoutUrl:latest.checkoutUrl,reused:true});
  const returnToken=makeCheckoutReturn({eventId:event.eventId,invitationId:invitation.invitationId});if(!returnToken)return json({error:'Secure checkout return could not be initialized.'},503);const site=(env('WILDONES_SITE_URL')||new URL(req.url).origin).replace(/\/$/,'');const attempt=Number(latest?.attempt||0)+1;const idempotency=`wildones-${event.eventId}-${invitation.invitationId}-${attempt}`.slice(0,255);const params={mode:'payment',success_url:`${site}/ticket-confirmed?session_id={CHECKOUT_SESSION_ID}&return_token=${encodeURIComponent(returnToken)}`,cancel_url:`${site}/events/${event.slug}?checkout=cancelled`,client_reference_id:invitation.invitationId,'line_items[0][quantity]':'1','line_items[0][price_data][currency]':'usd','line_items[0][price_data][unit_amount]':String(price),'line_items[0][price_data][product_data][name]':ticketProductName(event),'metadata[event_id]':event.eventId,'metadata[invitation_id]':invitation.invitationId,'metadata[application_id]':invitation.applicationId||'','metadata[user_id]':invitation.userId||'','metadata[purchase_type]':'admission','metadata[expected_amount]':String(price),'payment_intent_data[metadata][event_id]':event.eventId,'payment_intent_data[metadata][invitation_id]':invitation.invitationId,'payment_intent_data[metadata][application_id]':invitation.applicationId||'','payment_intent_data[metadata][user_id]':invitation.userId||'','payment_intent_data[metadata][purchase_type]':'admission'};
  try{const session=await stripePost('checkout/sessions',params,idempotency);const record={paymentId:session.id,eventId:event.eventId,invitationId:invitation.invitationId,applicationId:invitation.applicationId,userId:invitation.userId,status:'checkout_created',attempt,checkoutUrl:session.url,expiresAt:session.expires_at?new Date(session.expires_at*1000).toISOString():new Date(Date.now()+30*60000).toISOString(),amountTotal:price,currency:'usd',createdAt:new Date().toISOString(),updatedAt:new Date().toISOString()};await Promise.all([payments.setJSON(eventKey(event.eventId,session.id),record),payments.setJSON(latestKey,record)]);await writeAudit({eventId:event.eventId,action:'checkout.created',actor:'guest',targetType:'payment',targetId:session.id,detail:{invitationId:invitation.invitationId}});return json({ok:true,checkoutUrl:session.url,reused:false});}catch(error){return json({error:error.message||'Checkout could not be created.'},502);}
};
export const config={path:'/api/ticket/checkout'};
