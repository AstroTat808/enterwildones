import { isAdmin } from './_admin-auth.mjs';
import { blobStore } from './_blob-store.mjs';
import { env } from './_env.mjs';
import { getEvent, listEvents, toPublicEvent } from './_events.mjs';
import { json, readBody } from './_response.mjs';
import { sameOrigin } from './_security.mjs';
import { STORES } from './_stores.mjs';
import { listDrinkLedgers } from './_drink-ledger.mjs';

const REHEARSAL_STORE='wildones-rehearsal';
const ACTIVE=new Set(['paid','checked_in']);
const MANUAL_ITEMS=Object.freeze([
  ['invitation-approval','Invitation → approval → invite','Approve a controlled applicant and redeem the issued invitation.'],
  ['stripe-checkout','Stripe checkout','Complete a controlled Stripe sandbox checkout and confirm fulfillment.'],
  ['waiver-qr','Waiver → QR unlock','Sign the waiver and verify the QR becomes available only afterward.'],
  ['wallet-install','Apple Wallet','Install the resulting pass on a physical iPhone and open it successfully.'],
  ['gate-scan','Gate scan','Scan the ticket on a physical gate device and verify duplicate-scan handling.'],
  ['drink-package','Drink package purchase','Purchase a controlled drink package and verify the entitlement.'],
  ['bartender-redemption','Bartender redemption','Activate a wristband and redeem credits from the bartender console.'],
  ['refund','Admission + add-on refund','Refund controlled admission/add-ons and verify ticket and entitlement state.'],
  ['failed-payment','Failure drill','Exercise cancelled/failed payment behavior without issuing admission.'],
  ['offline-fallback','Event-day fallback','Verify manual token entry and the documented recovery path if camera/network access fails.']
]);

async function records(storeName,eventId){const store=blobStore(storeName),listed=await store.list({prefix:`${eventId}/`}),out=[];for(const {key} of listed.blobs||[]){const x=await store.get(key,{type:'json'}).catch(()=>null);if(x?.eventId===eventId)out.push(x);}return out;}
function configured(...keys){return keys.every(k=>Boolean(env(k)));}
function eventPrefix(eventId){return String(eventId||'').replace(/[^a-z0-9]+/gi,'_').replace(/^_|_$/g,'').toUpperCase();}
function stripeMode(){const key=String(env('STRIPE_SECRET_KEY')||'');return key.startsWith('sk_live_')?'live':key.startsWith('sk_test_')?'test':key?'configured':'missing';}
async function rehearsal(eventId){const stored=await blobStore(REHEARSAL_STORE).get(eventId,{type:'json'}).catch(()=>null);const saved=stored?.items&&typeof stored.items==='object'?stored.items:{};const items=MANUAL_ITEMS.map(([id,label,detail])=>({id,label,detail,status:['passed','failed'].includes(saved[id]?.status)?saved[id].status:'pending',note:String(saved[id]?.note||''),updatedAt:saved[id]?.updatedAt||null}));const passed=items.filter(x=>x.status==='passed').length,failed=items.filter(x=>x.status==='failed').length,pending=items.length-passed-failed;return{items,tester:String(stored?.tester||''),updatedAt:stored?.updatedAt||null,passed,failed,pending,percent:items.length?Math.round(passed/items.length*100):0,ready:failed===0&&pending===0};}

async function snapshot(event){const eventId=event.eventId;const [apps,invites,tickets,payments,entitlements,checkins,ledgers]=await Promise.all([records(STORES.applications,eventId),records(STORES.invitations,eventId),records(STORES.tickets,eventId),records(STORES.payments,eventId),records(STORES.entitlements,eventId),records(STORES.checkins,eventId),listDrinkLedgers(eventId).catch(()=>[])]);const appById=new Map(apps.map(x=>[x.applicationId,x]));const activeTickets=tickets.filter(x=>ACTIVE.has(x.status));const signed=activeTickets.filter(x=>x.waiverSignedAt).length;const checked=activeTickets.filter(x=>x.status==='checked_in'||x.checkedInAt).length;const admissionRevenue=payments.filter(x=>x.status==='paid'&&x.purchaseType!=='addon').reduce((n,x)=>n+Number(x.amountTotal||0),0);const addonRevenue=payments.filter(x=>x.status==='paid'&&x.purchaseType==='addon').reduce((n,x)=>n+Number(x.amountTotal||0),0);const activeDrink=entitlements.filter(x=>x.addonType==='drink_package'&&x.status==='active');const recentCheckins=activeTickets.filter(x=>x.checkedInAt).sort((a,b)=>String(b.checkedInAt).localeCompare(String(a.checkedInAt))).slice(0,12).map(x=>({ticketId:x.ticketId,guestName:appById.get(x.applicationId)?.fullName||'Guest',checkedInAt:x.checkedInAt}));const recentWristbands=ledgers.slice().sort((a,b)=>String(b.updatedAt).localeCompare(String(a.updatedAt))).slice(0,12);return{counts:{applications:apps.length,approved:apps.filter(x=>['approved','public_sale'].includes(x.status)||['approved','public_sale'].includes(x.decision)).length,invitations:invites.length,activeTickets:activeTickets.length,checkedIn:checked,unsignedWaivers:Math.max(0,activeTickets.length-signed),signedWaivers:signed,refundedTickets:tickets.filter(x=>x.status==='refunded'||x.refundedAt).length,activeAddons:entitlements.filter(x=>x.status==='active').length,activeDrinkPackages:activeDrink.length,activeWristbands:ledgers.filter(x=>x.wristbandActivatedAt).length,creditsRedeemed:ledgers.reduce((n,x)=>n+Number(x.creditsRedeemed||0),0, ),creditsRemaining:ledgers.reduce((n,x)=>n+Number(x.creditsRemaining||0),0),checkinRecords:checkins.length},revenue:{admissionCents:admissionRevenue,addonCents:addonRevenue,totalCents:admissionRevenue+addonRevenue},recentCheckins,recentWristbands};}

function readiness(event){const p=eventPrefix(event.eventId);const config={database:configured('DATABASE_URL'),admin:configured('WILDONES_ADMIN_KEY','WILDONES_ADMIN_SESSION_SECRET'),stripe:configured('STRIPE_SECRET_KEY','STRIPE_WEBHOOK_SECRET'),stripeMode:stripeMode(),turnstile:configured('WILDONES_TURNSTILE_SITE_KEY','WILDONES_TURNSTILE_SECRET_KEY'),email:configured('RESEND_API_KEY','WILDONES_EMAIL_FROM'),ticketSigning:configured('WILDONES_TICKET_QR_SECRET','WILDONES_TICKET_ACCESS_SECRET'),gate:configured('WILDONES_CHECKIN_KEY','WILDONES_CHECKIN_SESSION_SECRET'),bar:configured('WILDONES_BAR_KEY','WILDONES_BAR_SESSION_SECRET'),passport:configured('WILDONES_PASSPORT_SESSION_SECRET'),venue:configured(`WILDONES_${p}_VENUE_NAME`,`WILDONES_${p}_VENUE_ADDRESS`),wallet:configured('WILDONES_APPLE_PASS_TYPE_ID','WILDONES_APPLE_TEAM_ID','WILDONES_APPLE_WWDR_CERT_BASE64','WILDONES_APPLE_PASS_CERT_BASE64','WILDONES_APPLE_PASS_KEY_BASE64')};const blockers=[];for(const [key,label] of [['database','Database'],['admin','Admin authentication'],['stripe','Stripe'],['turnstile','Turnstile'],['email','Email'],['ticketSigning','Ticket signing'],['gate','Gate credentials'],['bar','Bar credentials'],['venue','Private venue']])if(!config[key])blockers.push(label);return{config,coreReady:blockers.length===0,blockers};}

export default async(req)=>{if(!isAdmin(req))return json({error:'Unauthorized.'},401);const url=new URL(req.url);const eventId=String(url.searchParams.get('eventId')||listEvents({includeHidden:true})[0]?.eventId||'');const event=getEvent(eventId);if(!event)return json({error:'Unknown event.'},400);
  if(req.method==='GET'){const [live,runbook]=await Promise.all([snapshot(event),rehearsal(eventId)]);return json({event:toPublicEvent(event),events:listEvents({includeHidden:true}).map(toPublicEvent),...readiness(event),live,rehearsal:runbook,generatedAt:new Date().toISOString()});}
  if(req.method!=='POST')return json({error:'Method not allowed.'},405);if(!sameOrigin(req))return json({error:'Origin not allowed.'},403);let body;try{body=await readBody(req);}catch{return json({error:'Invalid request.'},400);}if(body.eventId!==eventId)return json({error:'Event scope mismatch.'},400);const store=blobStore(REHEARSAL_STORE),current=await store.get(eventId,{type:'json'}).catch(()=>null)||{eventId,items:{}};const now=new Date().toISOString();if(body.action==='set_rehearsal'){const id=String(body.itemId||''),allowed=new Set(MANUAL_ITEMS.map(x=>x[0]));if(!allowed.has(id))return json({error:'Unknown rehearsal item.'},400);const status=String(body.status||'pending');if(!['pending','passed','failed'].includes(status))return json({error:'Invalid status.'},400);current.items={...(current.items||{}),[id]:{status,note:String(body.note||'').slice(0,500),updatedAt:now}};current.tester=String(body.tester||current.tester||'').slice(0,80);current.updatedAt=now;await store.setJSON(eventId,current);return json({ok:true,rehearsal:await rehearsal(eventId)});}if(body.action==='save_tester'){current.tester=String(body.tester||'').slice(0,80);current.updatedAt=now;await store.setJSON(eventId,current);return json({ok:true,rehearsal:await rehearsal(eventId)});}if(body.action==='reset_rehearsal'){await store.delete(eventId).catch(()=>{});return json({ok:true,rehearsal:await rehearsal(eventId)});}return json({error:'Unknown action.'},400);};
export const config={path:'/api/admin/readiness'};
