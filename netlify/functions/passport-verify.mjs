import { consumeMagicLink,createPassportSession,passportCookie } from './_passport-auth.mjs';
import { blobStore } from './_blob-store.mjs';
import { STORES,userKey } from './_stores.mjs';

function page(text,status=200){return new Response(`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>Wild Ones Passport</title></head><body style="margin:0;background:#050505;color:#f6f1e8;font-family:system-ui;display:grid;min-height:100vh;place-items:center"><main style="max-width:620px;padding:40px;text-align:center"><p style="letter-spacing:.25em;font-size:11px">ENTER WILD ONES</p><h1>${text}</h1></main></body></html>`,{status,headers:{'Cache-Control':'no-store','Content-Type':'text/html; charset=utf-8','X-Robots-Tag':'noindex, nofollow'}});}
export default async(req)=>{if(req.method!=='GET')return page('Method not allowed.',405);const token=new URL(req.url).searchParams.get('token')||'';try{const data=await consumeMagicLink(token);if(!data)return page('This Passport sign-in link is invalid, expired, or already used.',400);const user=await blobStore(STORES.users).get(userKey(data.userId),{type:'json'}).catch(()=>null);if(!user||user.status!=='active'||user.email!==data.email)return page('Passport identity could not be verified.',403);const session=createPassportSession(user);return new Response(null,{status:303,headers:{Location:'/passport','Set-Cookie':passportCookie(session),'Cache-Control':'no-store'}});}catch(error){console.error('Passport verification failed',error);return page('Passport sign-in is temporarily unavailable.',503);}};
export const config={path:'/passport/verify'};
