import { resolveEvent,toPublicEvent } from './_events.mjs';import { readInviteAccess } from './_invitations.mjs';import { existingTicketForInvitation,loadInvitation,ticketPriceCents } from './_payments.mjs';import { json } from './_response.mjs';
export default async(req)=>{if(req.method!=='GET')return json({error:'Method not allowed.'},405);const url=new URL(req.url);const event=resolveEvent({eventId:url.searchParams.get('eventId'),slug:url.searchParams.get('event')});if(!event)return json({error:'Event not found.'},404);const access=readInviteAccess(req,event.eventId);if(!access)return json({error:'Invitation access is missing or expired.',eligible:false},401);const invitation=await loadInvitation(event.eventId,access.invitationId);if(!invitation||invitation.userId!==access.userId||invitation.applicationId!==access.applicationId)return json({error:'Invitation access does not match this event.',eligible:false},403);const existing=await existingTicketForInvitation(event.eventId,invitation.invitationId);return json({event:toPublicEvent(event),eligible:event.ticketSalesOpen&&invitation.status==='redeemed'&&Number(invitation.maxTickets||1)===1&&!existing,priceCents:ticketPriceCents(event.eventId),currency:'usd',invitation:{invitationId:invitation.invitationId,status:invitation.status,maxTickets:invitation.maxTickets},existingTicketId:existing?.ticketId||null});};
export const config={path:'/api/ticket/access'};
