const dateFormatter=new Intl.DateTimeFormat('en-US',{month:'short',day:'numeric',year:'numeric',timeZone:'Pacific/Honolulu'});function updateEventCard(event){const card=document.querySelector(`[data-event-id="${CSS.escape(event.eventId)}"]`);if(!card)return;const date=card.querySelector('[data-event-date]');const status=card.querySelector('[data-event-status]');if(date&&event.startsAt)date.textContent=dateFormatter.format(new Date(event.startsAt)).toUpperCase();if(status){if(event.ticketSalesOpen)status.textContent='TICKETS OPEN';else if(event.applicationOpen)status.textContent='ACCESS OPEN';else status.textContent=String(event.status||'COMING SOON').replaceAll('_',' ').toUpperCase();}}async function hydrateEvents(){try{const response=await fetch('/api/events',{headers:{Accept:'application/json'}});if(!response.ok)return;const data=await response.json();for(const event of data.events||[])updateEventCard(event);}catch{}}hydrateEvents();